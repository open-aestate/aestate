<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.mapper.TravelgroupMapper">
    <resultMap id="BaseResultMap" type="com.atguigu.pojo.TravelGroup">
        <!--@mbg.generated-->
        <!--@Table t_travelgroup-->
        <id column="id" property="id"/>
        <result column="code" property="code"/>
        <result column="name" property="name"/>
        <result column="helpCode" property="helpcode"/>
        <result column="sex" property="sex"/>
        <result column="remark" property="remark"/>
        <result column="attention" property="attention"/>
    </resultMap>
    <sql id="Base_Column_List">
        <!--@mbg.generated-->
        id, code, `name`, helpCode, sex, remark, attention
    </sql>


    <!--auto generated by MybatisCodeHelper on 2021-07-29-->
    <delete id="deleteById">
        delete
        from t_travelgroup
        where id = #{id}
    </delete>

    <insert id="insertItemAndGroup">
        insert into t_travelgroup_travelitem (travelgroup_id, travelitem_id)
        VALUES (#{groupId}, #{itemId})
    </insert>

    <insert id="add" keyProperty="id" useGeneratedKeys="true">

        insert into t_travelgroup (code, name, helpCode, sex, remark, attention)
        VALUES (#{code}, #{name}, #{helpcode}, #{sex}, #{remark}, #{attention})
    </insert>

    <select id="findPage" resultMap="BaseResultMap">
        select *
        from t_travelgroup
        <where>
            <if test="dvalue!=null and  value.length>0">
                code=#{value} or name=#{value} or helpCode=#{value}
            </if>
        </where>
    </select>

    <select id="findById" resultType="com.atguigu.pojo.TravelGroup">
        select * from t_travelgroup where id=#{id}
    </select>

    <select id="findItemByGroup" resultType="java.lang.Integer">
        select travelitem_id from t_travelgroup_travelitem where travelgroup_id=#{id}
    </select>

    <delete id="delete">
        delete from t_travelgroup_travelitem where travelgroup_id=#{id}
    </delete>

    <update id="edit">
        update t_travelgroup
        <set>
            <if test="code !=null">code=#{code},</if>
            <if test="name !=null">name=#{name},</if>
            <if test="helpcode !=null">helpCode=#{helpcode},</if>
            <if test="sex !=null">sex=#{sex},</if>
            <if test="remark !=null">remark=#{remark},</if>
            <if test="attention !=null">attention=#{attention}</if>
        </set>
        where id=#{id}
    </update>

    <select id="findAll" resultType="com.atguigu.pojo.TravelGroup">
        select * from t_travelgroup
    </select>

    <resultMap id="getTravelgroupBySetmealId" type="com.atguigu.pojo.TravelGroup">
        <id column="id" property="id"/>
        <result column="code" property="code"/>
        <result column="name" property="name"/>
        <result column="helpCode" property="helpcode"/>
        <result column="sex" property="sex"/>
        <result column="remark" property="remark"/>
        <result column="attention" property="attention"/>
        <collection property="travelItems" select="com.atguigu.mapper.TTravelitemMapper.getTravelItemByGroupId"
                    column="id"></collection>
    </resultMap>
    <select id="getTravelgroupBySetmealId" resultMap="getTravelgroupBySetmealId">
        select t_travelgroup.*
        from t_travelgroup
        where id in(
        select travelgroup_id from t_setmeal_travelgroup
        where setmeal_id=#{setmealId}
        )
    </select>
</mapper>